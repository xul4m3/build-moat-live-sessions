# Dev / test only dependencies.
# Don't bake into runtime image — they expand attack surface and bloat image size.
#
# Local dev: pip install -r requirements.txt -r requirements-dev.txt
# Docker:    only requirements.txt goes into the final image.
-r requirements.txt

pytest==8.3.3
httpx==0.27.2
